#!/bin/bash
git config --global user.name "mtib"
git config --global user.email "mtb@tibyte.net"
git config --global core.editor "nvim"
git config --global github.user "mtib"
git config --global core.excludesfile "~/.gitignore"

